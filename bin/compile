#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found a dudewtf.txt"


#if dudewtf.txt is empty, abort the build
if [ ! -s $1/dudewtf.txt ]; then
  echo "dudewtf.txt was empty" | indent
  exit 1;
fi


cat $1/dudewtf.txt | sed -e "s/[Dd]udewtf/Btchwtf/g" > $1/btchwtf.txt
